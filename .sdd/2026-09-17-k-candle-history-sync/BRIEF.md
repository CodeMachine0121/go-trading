# 指名一段歷史，現在就把它抓回來 — Requirements Brief

## Goal

系統手上有兩種把 K 線抓回來的方式，而它們都**不讓你說要多久**：

- **定時那一輪**只抓最新的幾根，它的工作是跟上市場。
- **補缺口**（`POST /k-candles/backfill`）抓到系統設定的回補上限為止，
  而且刻意不接受呼叫端指定——那一條規則寫在 `KCandleBackfillRequest` 上，
  理由是「同一顆按鈕不該因為誰按而做不同的事」。

所以「我要 BTCUSDT 最近三十天的一分鐘 K 線，現在」這句話，今天講不出來。
唯一的辦法是改環境變數再重啟，而那會連帶改變每一次自動回補的行為。

這個切片多一條路：**指名一個交易標的與回溯天數，把那一段抓回來。**

## Requirements

### 一次同步要交代兩件事

- **哪一個交易標的**。系統沒登錄過的代號拒絕——沒有登錄就沒有市場，
  沒有市場就沒有來源可以問，而從代號長相猜一個是這個系統刻意不做的事。
- **回溯幾天**。從現在往回推這麼多天。

**粒度固定一分鐘一根，不接受指定。** 系統儲存的 K 線只有這一種粗細；
更粗的是算出來的，不是存下來的。給一個選項等於暗示還有別的答案。

### 抓的是那一整段，不是缺口

**已經有的那幾根也重抓，並覆蓋掉。** 這與補缺口正好相反，而那正是它存在的理由：
補缺口問的是「我漏了什麼」，這一支問的是「這三十天在不在、而且是對的」。
少了重抓，一段當初抓錯的歷史就永遠沒有辦法修正——那是唯一能修它的手。

### 回溯天數要有上限

一分鐘一根，三十天是四萬三千根，來源要分四十幾次才給得完。
沒有上限的話，一句話就能讓系統對來源連打幾個小時。

- **至少一天**。零天或負數不是一個小的要求，是一個不成立的要求。
- **不超過設定的上限**（預設九十天），超過即拒絕並說出上限是多少——
  被擋下來的人要知道該改成什麼，而不是自己試。

### 它會跑很久，而且就讓它跑

九十天大約一百三十次來源往返，可能好幾分鐘。這一支是**手動工具**，
呼叫它的是 Postman 或一個人，不是畫面。所以它就同步地跑完再回覆，
不做成「先收下、之後再問」——那一套是為了畫面上的等待而存在的，這裡沒有畫面。

### 交代它做了什麼

回的是**與既有回補一字不差的那份報告**：存了幾根、跳過哪幾根與為什麼、
來源有沒有答話。同一件事不該有兩種說法。

### 不順便做的事

- **不碰觀察清單**。指名的標的不在清單上也照樣抓得動——看得到圖就該補得動，
  這條沿用既有的手動回補。
- **不推定休市**。一個標的的沉默是那一個標的的，不是整個市場的。同樣沿用。

## Examples (Specification by Example)

### Rule: 指名標的與回溯天數，把那一段抓回來

| # | Given | When | Then |
|---|---|---|---|
| 1 (happy) | BTCUSDT 已登錄，系統裡一根都沒有 | 同步 BTCUSDT、回溯 30 天 | 抓回這 30 天的一分鐘 K 線，報告說存了幾根 |
| 2 (happy) | BTCUSDT 已有最近 3 天的 K 線 | 同步 BTCUSDT、回溯 30 天 | **整整 30 天都重抓**，既有那 3 天被覆蓋 |
| 3 (boundary) | 同上 | 看它抓了什麼粗細 | 一分鐘一根；請求裡沒有地方可以指定別的 |
| 4 (exception) | 系統沒登錄過 FOOBAR | 同步 FOOBAR、回溯 30 天 | 拒絕，說這個代號沒有登錄 |
| 5 (exception) | 代號是空白 | 同步 | 拒絕，說那不是一個代號 |

### Rule: 回溯天數有上下限

| # | Given | When | Then |
|---|---|---|---|
| 6 (boundary) | 上限是 90 天 | 回溯 1 天 | 照做 |
| 7 (boundary) | 同上 | 回溯 90 天 | 照做 |
| 8 (exception) | 同上 | 回溯 91 天 | 拒絕，並說出上限是 90 天 |
| 9 (exception) | 同上 | 回溯 0 天 | 拒絕 |
| 10 (exception) | 同上 | 回溯 -1 天 | 拒絕 |
| 11 (exception) | 同上 | 沒給回溯天數 | 拒絕——沒有預設值，這一支的重點就是你要說多久 |

### Rule: 來源不答話時說清楚是誰的問題

| # | Given | When | Then |
|---|---|---|---|
| 12 (exception) | 來源連不上 | 同步 | 回「稍後再試」那一類，不是「你寫錯了」那一類 |
| 13 (happy) | 來源答了，但那幾天市場沒開 | 同步 | 不算失敗：報告說來源被問過了、存了 0 根 |

### Rule: 報告與既有回補一字不差

| # | Given | When | Then |
|---|---|---|---|
| 14 (happy) | 抓回來的那批裡有一根壞掉 | 同步 | 報告列出那一根與它違反了哪一條規則，其餘照存 |

## Out of Scope

- **指定粒度**。存的只有一分鐘一根。
- **指定起訖時刻**（而不是回溯天數）。要那個的人是在做別的事——
  他要的是「那一天」，不是「最近多久」。
- **非同步／先收下再問進度**。這是手動工具，沒有畫面在等。
- **前端**。這一支只進 Postman 集合。
- **改動既有的 `POST /k-candles/backfill`**。它那條「回溯長度不由呼叫端指定」的規則
  完好如初，因為這是另一件事。

## Open Decisions

- 上限九十天。一分鐘一根、九十天約十三萬根、一百三十次來源往返。
  再大就該先把「邊抓邊存」做出來，而不是先把上限調高。

## Context / Background

**為什麼不是在既有那一支加一個欄位。** 那一支的身分就是「回溯長度由系統決定」，
它的請求物件上寫著這句話當理由。加一個選填欄位會讓那句話變成半真的——
而半真的規則比沒有規則更難維護。兩件事分成兩條路，各自的規則都說得完整。

**為什麼重抓而不是只補缺口。** 「補缺口」已經有人做了，再做一次沒有意義。
這一支唯一多出來的能力，正是**重新抓一段已經有的歷史**——當初抓錯、來源事後修正、
或只是想確認它是對的，今天都沒有任何辦法。
